 MapJSONKeys←{
 ⍝ Copy values from ⍺ into ←
 ⍝ Changing keys from (ka←⊣/⍵) into (kr←⊢/⍵)
     0=≢⍺:⍺
     r←⎕JSON¨(≢⍺)⍴⊂'{}'
     (ka kr)←⍵
     ⎕JSON ⎕JSON⊂,2,⊂kr⍪⍺∘.⍎ka   ⍝ Use ⎕JSON Export Wrapper to convert nested matrix
 }
