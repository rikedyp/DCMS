 MapJSONKeys←{
 ⍝ Copy values from ⍺ into ←
 ⍝ Changing keys from (ka←⊣/⍵) into (kr←⊢/⍵)
 ⍝ ⍺ :             vec of ref :: values to be copied
 ⍝ ⍵ : nested vec of char vec :: name mappings (from to)
     0=≢⍺:⍺
     r←⎕JSON¨(≢⍺)⍴⊂'{}'
     (kf kt)←⍵   ⍝ Key From; Key To
     _←⍺(kf CY kt)¨r
     r
 }
